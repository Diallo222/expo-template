# Public env vars (embedded at build time; never put secrets here).
EXPO_PUBLIC_API_URL=https://api.example.com
# Set to true to sign in without a backend (issues mock tokens).
EXPO_PUBLIC_MOCK_AUTH=false
